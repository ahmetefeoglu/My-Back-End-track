function findLargestDenom(arr_num) {
  var denom = [];
  arr_num.forEach(function(num,index) {
    denom.push(number);
    for(i = 0; i < arr_num.length; i++) {
      if (!arr_num[i] % number === 0) {

        break;
      }
    } 
  });
  return denom;
}

console.log(findLargestDenom([2,4,8,12]));